{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "preLaunchTask": "tsc build",
      "program": "${workspaceFolder}/dist/main.js", // Adjust the path to your compiled JS file
      "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}